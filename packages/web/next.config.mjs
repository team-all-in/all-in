/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'avatars.slack-edge.com', 'cdn.discordapp.com'],
  },
};

export default nextConfig;
